no_tty: true

pre-commit:
  commands:
    branch-protection:
      run: just check-branch
    local-gates:
      run: just pre-commit-fast

pre-push:
  commands:
    local-gates:
      run: just pre-push
      use_stdin: true
