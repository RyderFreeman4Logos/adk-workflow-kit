schema_version = 1

[workflow]
id = "issue-269-downstream"
version = "1"
entry = "agent"

[[nodes]]
id = "agent"
kind = "agent"
model = { role = "worker", id = "consumer-fake", version = "1" }
tools = [{ id = "echo", version = "1" }]
skills = [{ id = "demo", version = "1" }]

[[nodes]]
id = "terminal"
kind = "terminal"

[[edges]]
from = "agent"
to = "terminal"
